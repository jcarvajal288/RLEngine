#include "GameWindow.hpp"

namespace rlns
{
    namespace view
    {
        /*--------------------------------------------------------------------------------
            Function    : GameWindow::update
            Description : Draws the player's current view to the main Display
            Inputs      : None
            Outputs     : None
            Return      : void
        --------------------------------------------------------------------------------/
        void GameWindow::update() const
        {
            DisplayInfo disp(topLeft(), bottomRight(), focalPoint);
            model::GameData::get()->draw(disp);
        }*/
    }
}
